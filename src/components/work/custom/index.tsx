import ParticleLife from './particle-life';

export const CustomProjectComponents: Record<string, React.ReactNode> = {
    "particle-life-131": <ParticleLife />,
    // "weekend": <WeekendScene />,
};
